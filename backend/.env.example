# Django
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_SECRET_KEY=change-me

# Frontend / CORS
CORS_ALLOW_ALL_ORIGINS=true

# Database
# Use sqlite for the default lightweight local setup.
# Set DB_ENGINE=postgres after starting Docker Postgres.
DB_ENGINE=sqlite
POSTGRES_DB=tox_db
POSTGRES_USER=tox_user
POSTGRES_PASSWORD=tox_password
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5433

# Google OAuth
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret

# Email OTP / Resend
RESEND_API_KEY=re_your_resend_api_key
DEFAULT_FROM_EMAIL=ToxicWaste <onboarding@resend.dev>
OTP_EXPIRY_MINUTES=10
OTP_RESEND_COOLDOWN_SECONDS=60
OTP_MAX_ATTEMPTS=5

# External APIs
TMDB_API_KEY=YOUR_TMDB_API_KEY_HERE

# Optional S3-compatible storage / MinIO
# Set USE_S3=true after starting Docker MinIO.
USE_S3=false
AWS_ACCESS_KEY_ID=tox_minio_admin
AWS_SECRET_ACCESS_KEY=tox_minio_password
AWS_STORAGE_BUCKET_NAME=tox-media
AWS_S3_REGION_NAME=us-east-1
AWS_S3_ENDPOINT_URL=http://127.0.0.1:9000
AWS_S3_ADDRESSING_STYLE=path
AWS_QUERYSTRING_AUTH=false
AWS_S3_FILE_OVERWRITE=false
AWS_S3_URL_PROTOCOL=http:
AWS_S3_CUSTOM_DOMAIN=127.0.0.1:9000/tox-media
